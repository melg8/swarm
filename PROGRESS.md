# PROGRESS

Generated: 2026-09-19 10:11:43Z

## Milestone ladder

**M0 (elven lands autonomy):** done
**M1 (soak proof: level 1 to 20 unattended):** red (last run FAIL)
**M2 (first profession (class transfer at 20)):** pending
**M3 (the 20 to 40 band):** pending
**M4 (second profession (class transfer at 40)):** pending
**M5 (the 40 to 60 band):** pending
**M6 (the integral 1 to 60 run):** pending

## Soak metrics (last 10 runs)

| Date | Scenario | Duration | Start | End | XP/h | Deaths | Adena | Stuck | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-13T00:21:42Z | class-transfer | 471s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |
| 2026-09-13T00:36:25Z | class-transfer | 583s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |
| 2026-09-13T00:55:29Z | class-transfer | 468s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |
| 2026-09-13T01:10:06Z | class-transfer | 489s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |
| 2026-09-13T01:20:20Z | class-transfer | 424s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |
| 2026-09-13T01:36:11Z | class-transfer | 587s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |
| 2026-09-13T01:51:49Z | class-transfer | 585s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |
| 2026-09-13T02:23:24Z | class-transfer | 1190s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |
| 2026-09-13T02:53:30Z | class-transfer | 905s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |
| 2026-09-13T03:18:59Z | class-transfer | 1198s | 20 | 20 | 0 | 0 | 100000 | 0 | FAIL |

## Recent commits

```
1370985 pathfind: the village layer sandwich evidence pins the refused click root cause - the plaza deck stacks the open deck top over the water floor 872 units apart, the teacher hall block stacks the roof band, the interior floor and the water floor with the interior flip margin down to 164 units, the shop interior cells are partially walled in the pack (the nswe mask 7, the east wall closed) and the deck steps 64 units between the plaza and the gate corridor inside one pack; the server resolves every click target and getValidLocation line step layer by the nearest z, so a pack vintage disagreement flips the resolved layer, the line check refuses, the click collapses onto the walker and the deployed build family answers ActionFailed - the wholesale refusals the official client's mouse clicks met on the same cell; the full analysis lands as Round 85 of the development log with the owner side cure (the geodata vintage alignment, the master direct-movement fallback) and the bot side answer (the route following claim transport of the previous commits)
5cc9056 hunt: the move start watchdog forces the recovery ladder the moment a walk click proves dead - noteMoveStart arms a 3s deadline per standing click, the position change or the server movement broadcast clears it and the deadline passing on the baseline cell forces the stuck verdict (no more standing out the 15s first window on a click nothing answered); the refusing pocket verdict (the refusal evidence on the first refusal cell after the varied aims are spent) hands the leg to the cursor key escape with its route following claim ladder, the follower drives the claims while the escape holds the leg and stands its clicks down, the direct routed leg arms the escape on a silent hop instead of burning the 45s window and the direct zone legs backdate their stall so a dead leg click re-discovers itself in seconds
19025d4 docs: the webui reference documents the debugging surface round - the pathfind link button joins the toolbar inventory (the URL pieces, the camera framing math, the base address override), the walk path view explains the hover only waypoint labels and the dump timing suffixes (the started line, the t+/leg numbers of the passed waypoints, the walking number of the aimed one, the 250 ms tick resolution), the cursor chip section describes the footer readout and the ctrl+c copy with the viewer paste contracts (the wp marker lines bind the first three numbers, the bare pair inherits the z)
a3eac90 hunt: the cursor key escape walks the planned route, not the straight chord - the claimed ValidatePosition steps of a refusing cell escape follow the plan polyline from the current cursor (the pathfind route the leg already walks), interpolated into run-speed strides, water guarded per stride and capped at cursorEscapeRouteMax (2500 units, one direct hop of ground) so an escape stays a pocket recovery and the claims never carry the character across the map; the straight line ladder stays as the planless fallback; the bend closure keeps the route bend points among the steps so the next segment leaves from the bend and not from a cut corner
3f75ccc webui: the walk plan coordinates draw on hover only and the status bar answers the cursor - the constant per waypoint (x, y) labels littered every planned walk, the waypoint dot now prints its full x y z triple (the dump walk line format, paste ready) only while the pointer holds it, the footer gains the cursor chip (the world point under the mouse, upgraded to the triple on a hovered waypoint, the tabular monospace readout) and the ctrl+c over the map copies the chip values through the dump clipboard fallbacks (the selection aware fall through never masks the browser copy: a shortcut with the pointer off the map or an active text selection goes through untouched, the handled copy flashes the chip), leaving the map clears the chip; the 3D viewer coordinate inputs accept the bare x y pair of the cursor copy (the z inherits from the other line - the map knows no terrain height under the cursor, the route search snaps to the mesh) and the wp prefixed dump lines parse again (the first three numbers after the wp marker bind - the timing suffix of the new dump carries numbers of its own, the last three of the line contract broke for the exact lines the dump emits); the repro_map_render harness drives the surface end to end (the constant label absence, the hover label and chip, the copy shortcut with the flash and restore, the pair copy, the modifier fall through, the leave reset)
3b01c61 state: the gofmt-spaces gate folds the timing fill loop header back onto one line - the hand wrapped for clause of the walk plan timing tracking carried a two line body-less header the formatter of record rejoins, no behavior change
e39d4ef webui: the pathfind link button freezes the running walk into the 3D navmesh viewer link - one click copies the URL the -show-navmesh page answers itself with, the owner experiments in the 3D world or attaches the reproducible route to an agent report without a single extra click; the link rides the planning origin as from and the final destination as to of the published walk plan, the tiles around the pair (the bounding box grown by half a tile reproduces the owner example link's 20_19..21_20 block exactly), the swim filter and the scale/geom/path defaults of the viewer toggles, and the camera pose computed with the viewer's own framing math (the three quarter orbit south east of the route midpoint, the analytic yaw = atan2(-dx, -dy) and pitch = atan2(height delta, flat distance) of frameInitialTiles, the camera height above the route endpoints); a snapshot without a published plan opens the viewer bare (no from/to/cam/tiles, the defaults stay); the base address defaults to the documented local http://127.0.0.1:8082/ and the shift click asks for a different one, remembered in the localStorage; the repro_hud harness pins the URL structure end to end (the pair, the camera orientation and height, the tile block, the defaults, the bare snapshot)
30074c9 webserver: the state dump walk plan carries the per waypoint timing - the stuck point shows its cost, not just its name - the tracker records the observed arrival of every waypoint (walkWpAt fills when the follower cursor of the same route republish moves past i, the zero point walkPlanStart opens on the first publish, a fresh route or a cursor that moved back restarts the view, a mid walk publish with the cursor already ahead pre-fills the passed prefix) and the last walk record copies the timing view so a finished walk keeps its leg durations; the dump prints the started line (the zero point, the last seen moment, the time on the walk), the passed waypoints as (passed, t+10.4s, leg 5.2s) and the aimed one as <-- TARGET (walking 45.2s) - the last plan measures the aimed leg to the moment the plan ended, the sub minute durations keep the tenth of a second and the longer ones fold into the minute shape; the dump parser needs no change (the wp lines keep the leading x y z triple), the snapshot fields ride the Go side dump only (json -) and the wire payload stays byte identical; the tests pin the timing tracking (the advance, the equal republish, the fresh route, the record copy) and the section formatting (the suffixes, the minute fold, the untimed shape)
98bbe9d agents: the owner session limits and the fresh subprocess study land in AGENTS.md, the pre-split operational nuggets return
35ad87f hunt: the equip chain rides the acceleration limit the server really allows - every independent use item request goes out in one tick and only the shared state waits - the limit is pinned by two source facts of the deployed Mobius C1 module: the UseItem flood protector is disabled (FloodProtectorUseItemInterval = 0) so no rate limit sits between the requests, and the packet executor is one shared ThreadPoolExecutor (commons/network/packet/PacketExecutor.java) that runs every received packet as its own pool task, so two packets of the same client may execute concurrently and in either order - no same client ordering guarantee; that makes the write set the binding constraint: requests racing on the same item (a second request toggles the first one's effect back) or on the same paperdoll slots (the pair swap refill, the one-piece drop follow up, the shield slot behind a two hand weapon) can interleave on the server and cancel each other, while independent slots never touch the same state; gear.BurstUpgrade plans on a simulated copy of the equipment with the server write semantics (equipWriteSlots: the landing slot plus the displaced pieces, the quiver rides beside the worn bow) and returns the maximum independent prefix of the chain - the whole starting bag on an empty paperdoll dresses in one tick instead of one tick per piece, the pending single slot confirmation gate grew into the per item map (inventoryItemAllowed: the same item hold plus the write set overlap, the confirmed and expired pendings prune on the check) shared by the manual commands, the starter destroys keep their fixed two second window (the server refuses fast destroys) and the lure arm gates its bow and quiver steps per item; the acceptance suite gains the full-dress scenario (temp11, eleven wearable pieces injected into the bag, nothing worn): the world entry burst must put every piece on its paperdoll slot within the ten second window of the world entry - the retired fixed pause needed over twenty seconds for the same bag - with the checks online, dress and fast latching through the monitor watch; the tests pin the burst planner (the whole bag in one plan, the pair swap cut, the one-piece and shield drop chains cut, the mixed burst), the write sets, the one tick dress on the live loop, the gate granularity (the disjoint manual and auto requests ride one tick, the racing ones defer) and the slow dress failing the window
c3f65a8 hunt: the auto equipment dresses the character at the server confirmation pace - the fixed two second pause between the use item requests is gone: the deployed build disables the UseItem flood protector (FloodProtectorUseItemInterval = 0, the retail matching config of the official FloodProtector.ini) and the UseItem packet never touches the one second PlayerActionFloodProtector of the attack and select requests, so the shared inventory confirmation gate alone paces the chain - an unconfirmed equip holds the next request (the same slot pair swap can never race) and its flip releases the next upgrade on the very next tick, a character entering the world with the outfit in the bag wears the whole set within one tick per piece instead of one fixed pause per piece; the starter kit destroys and the shop unequip retries keep their fixed windows (the server refuses fast destroys with its own message); the tests pin the burst semantics (the in flight hold, the confirmation release, the four slot world entry dress) and the pair swap runs without the pacing backdating
700334a acceptance: the scenario card carries its bots and the click jumps to the bot - the owner reads which temp bot (or bots, the payload grows the bots list for the multi bot scenarios) drives the test right on the card (the pill chips with the live status dot of the registry state) and one click on the card or the chip switches everything at once: the sidebar flips to the tests view with the plaque selected and scrolled into sight, the map tab activates so the world view of the bot lands on the screen right away, the HUD the event stream and the proxy target rebind through the selectBot side effects; the run button keeps its own action through the stopPropagation so the new card click never masks the restart; the headless harness rechecked the jump end to end (the chip click and the card click land on the right plaque, the run click leaves the selection alone, the payload answers bots alongside the account)
14ff59b agents.md: the subprocess study lands - the 09-18 survival window note grows into the fresh 09-19 study: every detached variant dies at the end of the launching tool call (setsid own session with PPID 1, the clean env, the renamed binary, the unshare stays forbidden), the death tracks the session bookkeeping and not the process tree (the agent-browser daemon and its Chrome keep living across calls), the working pattern runs the long operation inside one tool call - start the server, wait for the port, run the probes, kill the server, print the results, with the harness script kept under scripts for reproducibility
5acc71d webserver: the scenarios earn their own top tab and the bot lists scroll again - the scenario panel squeezed into the two hundred pixels of the sidebar column (the run buttons cramped, the check lists clipped, the hover tooltip fought the panel edge), it moves into the Scenarios section beside Map, Log and Stats now (the wrap centers one wide column, the grid folds the scenarios into the card columns the width carries, the head row carries the title and the run all buttons) and the tab follows the payload - the processes without the acceptance manager never show it, an active tab falls back to the map when a poll loses the tests, the empty manager keeps the no tests hint; the sidebar TESTS view keeps the temp bots alone (the whole tab hides while none runs, the active choice falls back to LIVE); the bot lists never scrolled at all - the bot section was an unbounded flex item that grew with its content and pushed the legend past the viewport while the page body hides its own overflow, the section owns the flexible middle now (the bounded flex chain runs section to view to list) and both views scroll inside their own box while the legend stays pinned at the bottom; the verification ran the real server inside single tool calls with a fake fleet of forty bots and the real acceptance manager (the detached processes die at the call end whatever the pid trick, the plain page wheel control proved the headless wheel ignores the pointer position - the automation artifact, the box itself scrolls), the screenshots and the metrics pin the new tab and the scroll chain
0ad8708 pathfind: the funnel clearance shrinks the portal span ends a wall abuts and the corridor region shortcut threads the strips the chain left out - the raw answer of the owner route carried the strip joint weave twice over: the offsetPortal pulled every span end inward by the capsule radius and the 16 unit joints of the merged mesh strips shrank into pinholes the funnel pivoted through at every height run edge (81 raw waypoints, 26 sharp turns, 1348 summed degrees on the owner pair), and behind that the slope strips staircase one cell aside per strip so the one strip wide corridor chain imprisoned the exact funnel into sliding span pivots no chord pass could fold (the chords left the chain union); the shrink reads the shared edge continuation per span end now - the polygon walled at the continuation cell or the polygon corner walled along the perpendicular side pulls, the open continuation keeps its extent (spanEndWall, the eroded union portal) and the open terrain walks straight (56 raw waypoints, 16 turns, 857 degrees); the shortcut pass owns the corridor prison: the search corridor grows into the priced walk region (corridorRegion, the flood costs the search step and caps at corridorSlack, the avoid queries keep the bare chain) and the greedy farthest visible walk folds the funnel pivots into the longest chords that walk the region through the real link spans (regionChord, the rectangle exit raycast crossing only open spans of region polygons, the stacked layers ride the crossed links) and keep the clearance from the visited walls - the armed guard answers the walls instead of the mesh spans as before; the old chord portal crossing pass retires (the chain portal order was the prison), the owner pair answers 50 waypoints with the 1937 unit diagonal the S bends blocked and the regression worlds pin the new contracts (the open span end walks straight, the L corner keeps the funnel pivot under the capsule, the guard overrules the mesh spans); agents.md carries the fresh session limits (the 2 hour life, the mandatory 1h45m stop, the timer reset on every owner prompt, the session stamp file) and repro_rawpath runs one query through the viewer filter with the turn and length stats
61dc6c9 webserver: the viewer routes the pasted walk plan pair - the result panel carries the from and to inputs that read the walk plan log lines (the last three numbers of the pasted line bind, the wp prefixes and the TARGET markers fall away, the bare x y z triple passes through); the route the pair button and the Enter key ask the server right away, the missing to line arms the start marker the second double click finishes; the live pair mirrors back into the inputs (the double click search and the clear fill them too, the pair stays copyable from either side)
1838bd6 pathfind: the coarse component merge keeps the best settled g - the settled component merge skipped the cheaper entry the f order popped later (the crossings of one cluster carry different entry heuristics, the far entry pops first with the lower f while the cheap one stood beside it unexpanded, the walk plan carried the hook into the harbor and back out - the coarse chain walked two thousand units north, retraced them and only then took the honest direction); the merge re-arms the component on the strictly cheaper g now and the closed states re-open when the re-armed expansion relaxes them; the found hierarchical answer meets the exact competitors: the flat search within its standard budget serves the corridor when it reaches and walks shorter (the river valley chain the portal straight lines underpriced refined a quarter past the shore answer), the capped flat search hands the contest to the confined search over the chain clusters (the same set the refinement threads, the full polygon freedom, the crossings the representative hops missed - the ford pair answer dropped from 82.7k to 76.8k zone cost); the corridor comparison prices both answers under the search filter (the center walk weighted polyCost, the funnel approximation the waypoints refine); the regression pins moved with the honest answers: the ford contract carries the sub per mille search noise floor now (the one unit tolerance rode the cache warmth of the query order, both answers dropped 8 percent), the town ban pin expects the moat detour within the honest margin of the through town optimum (the contest made the free answer honest, the ban cannot shorten it anymore)
4f9aadd pathfind: the bot walk plans serve from the mesh only - the live walk plan the dump carried folded the correct mesh funnel answer into four straight grid legs (the grid capsule post pass of the clearedWaypoints ran ApplyPath and ShortenPath over the funnel waypoints with the geodata oracle, the grid walks the unzoned river beds and the dropped walls at their own world view, so the shore detour the mesh priced collapsed into the straight line through it - the state dump plan aimed at a waypoint the mesh route never holds); the navigator serves the mesh answer as the search produced it now - the funnel pivots and the corridor geometry shortcut pass own the wall avoidance, no post pass folds the route across the corridor, the grid engine never plans a route (the owner directive: only our mesh pathfind) and stays the validation seam it already was (ValidateClick, the sight lines, the water rasters, the deck heights); the hunt filters arm the C1 water zone pricing the viewer swim filter serves (the same zone cuboids, the same 2.3 ratio) so the bot plan and the viewer answer carry the identical pricing; the mesh partial corridors surface directly (Found=false Partial=true, the closest reachable point) without the engine confirmation detour - the manual walk walks the partial corridor instead of falling to the server straight line, a missing tile answers the honest error; tools/repro_gamepath runs one walk query through every planner configuration so the dump plan compares against the mesh answer side by side
3c2df35 agents.md: the reaper now kills the setsid detached processes too - the own session viewer of this session died within 20 seconds every restart, the unshare escape answers operation not permitted; the practical leftover is the 15-20 second survival window (the endpoint curl checks fit it, nothing longer), the long running servers stay the owner's job
0226867 webserver: the viewer camera residency bounds the full map memory - the bare -show-navmesh boot and the all box built every tile of the 164 tile world and the built variants never freed (the visible=false flag kept the surface, wall and connection buffers resident, both geometry variants cached per tile) - the browser carried the gigabyte scale buffers and hit the system limit against the swarm server; the residency manager keeps the 8 tiles nearest the camera loaded, disposes the rest (the geometry and material dispose walks the draw call tree, the built cache clears) and re-ranks on a 600 ms render loop tick - the checkboxes stay the mask (an unchecked row never loads, a checked far row reads far), the route overlays are independent of the tile builds so a path still draws beyond the resident tiles; the variant switch frees the old build instead of caching both, the small selections keep the classic load everything behavior
```
